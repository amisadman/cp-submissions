#include<bits/stdc++.h>
using namespace std;


void solve() {
    int x, y; cin >> x >> y;
    cout << ((y >= -1) ? "YES" : "NO") << "\n";
}
int32_t main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t; cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}